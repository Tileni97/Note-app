import "../styles/LoadingIndicator.css";

const LoadingIndicator = () => {
  return (
    <div className="loading-container">
      <div className="loader"></div>
    </div>
  );
};

export default LoadingIndicator;
// The LoadingIndicator component is a simple component that displays a loading spinner. It is used to indicate to the user that the application is loading data. The component is styled using CSS, which is defined in the LoadingIndicator.css file.
